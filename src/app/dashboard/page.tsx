import { ModeToggle } from '@/components/darkmode/ModeToggle';
import React from 'react';

const page = () => {
  return (
    <div>
      <nav>
        <ModeToggle />
      </nav>
      Landing Page
    </div>
  );
};

export default page;
